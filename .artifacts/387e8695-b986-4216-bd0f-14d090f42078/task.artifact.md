# Task: Optimize YouTube Direct Launch

- [x] Modify `onMediaClick` in `BiographyScreen.kt` to use `vnd.youtube:$videoId`
- [x] Remove redundant Intent flags and `setPackage` logic refinement
- [x] Verify that `<queries>` is NOT added to `AndroidManifest.xml` (cleanup if previously added)
- [/] Manual verification of direct video playback (Ready for user testing)
